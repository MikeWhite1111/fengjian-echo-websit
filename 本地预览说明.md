# 本地官网

双击桌面「打开封缄回响官网」即可观看。启动器自动检测并静默启动服务，重复打开复用同一个进程。

固定地址：http://127.0.0.1:4174/#home

- 服务只对这台电脑开放，不是公网地址。
- 关闭浏览器不会主动停止服务；重启电脑后再次双击即可启动。
- 不注册开机自启，不修改代理、DNS、防火墙。
- 若 4174 被其他程序占用，会显示错误，不会终止其他程序或跳到其他端口。
- 网站改动后需要重新构建；预览读取 `dist/client`，设置 `no-store` 避免旧构建缓存。
- 启动日志位于 `logs/preview.stdout.log` 和 `logs/preview.stderr.log`。
- 如需停止，在任务管理器中核对命令行含本项目 `scripts/local-preview.mjs` 的 Node 进程后结束它，不要结束其他 Node 进程。

封面保留原始图片颜色；标题使用本地志莽行书，授权见 `public/assets/fonts/OFL-ZhiMangXing.txt`，来源为 https://github.com/google/fonts/tree/main/ofl/zhimangxing 。字体文件未修改。

纸面角纹直接引用现有 `opening-story-zoige-blue.png` 的卷云、经幡和河湾局部，通过 CSS 裁切与边缘渐隐呈现。封面不透明度 32%–35%，故事阶段降到 25%–28%，产品阶段淡出；移动端仅保留两处角纹。不改原始图像文件。
